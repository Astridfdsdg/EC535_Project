Instructions on how to run game:

1. Copy all files into a directory
2. Run qmake
3. Run make -j4
4. Copy the resulting binary onto the BeagleBone
5. Run chmod +x paddle_ball_bricks_powerups  (if needed)
6. Run QT_QPA_PLATFORM=linuxfb QT_QPA_FB_HIDECURSOR=1 ./paddle_ball_bricks_powerups
